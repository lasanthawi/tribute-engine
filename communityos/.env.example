# Supabase
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
SUPABASE_STORAGE_BUCKET=communityos-assets

# Telegram bot / Mini App
TELEGRAM_BOT_TOKEN=123456:replace-with-bot-token
# TELEGRAM_BOT_USERNAME and NEXT_PUBLIC_TELEGRAM_BOT_USERNAME must be set to the
# SAME bot username. Next.js only exposes NEXT_PUBLIC_-prefixed vars to the
# client bundle, so the server-side referral link builder and the client-side
# share/copy buttons each need their own copy of the value.
TELEGRAM_BOT_USERNAME=communityos_bot
NEXT_PUBLIC_TELEGRAM_BOT_USERNAME=communityos_bot
MINI_APP_URL=https://your-communityos-domain.vercel.app
TELEGRAM_WEBHOOK_URL=https://your-communityos-domain.vercel.app
TELEGRAM_WEBHOOK_SECRET=replace-with-random-webhook-secret

# Cron/API protection
CRON_SECRET=replace-with-long-random-secret

# Platform admin setup
# Preferred: insert admin users into the platform_admins table after first login.
# Optional seed helper: comma-separated Telegram numeric IDs for your own bootstrap script.
ADMIN_TELEGRAM_IDS=123456789

# AI provider gateway
# Configure providers from /admin (provider, model, fallback priority, enable/disable).
# Set whichever keys you actually use as plain env vars below — the admin UI only ever
# stores the *name* of the env var (e.g. ANTHROPIC_API_KEY), never the key value itself.
# The gateway works with zero providers configured: AI calls simply fall back to
# templated text.
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
GEMINI_API_KEY=
